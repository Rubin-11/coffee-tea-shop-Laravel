# План: Feature-тесты (авторизация, ЛК, страницы)

- **Статус:** активный
- **Создан:** 2026-08-30
- **Цель:** Покрыть Feature-тестами всё, что добавили после вёрстки: авторизацию, восстановление пароля, ЛК/заказы, страницы. Критерий готовности: `tests/Feature` существует, тесты зелёные, HTTP 500 нигде не возвращается.

## Контекст

- Сейчас тестов 286, все — Unit (модели, сервисы). Папки `tests/Feature/` нет, хотя `phpunit.xml` на неё ссылается.
- База для Feature готова: `TestCase` с `RefreshDatabase` + хелперы `createUser`, `createProduct`, `createCategory`, `actingAsUser`, `actingAsAdmin`, тестовая БД `coffee_tea_shop_test`.
- Новый код для покрытия: `AuthController`, `ProfileController`, формы auth (4), страницы pages/* (6), orders/index+show, profile/index, миграция `password_reset_tokens`.

## Шаги

1. [ ] Создать `tests/Feature/AuthTest.php` — регистрация:
   - успех: юзер в БД, пароль захэширован, редирект в ЛК
   - дубль email → ошибка валидации
   - короткий пароль / несовпадение паролей / невалидный email → ошибки
2. [ ] Вход и выход:
   - успешный вход, редирект в ЛК
   - неверный пароль / неверный email → ошибка «Неверный email или пароль»
   - «запомнить меня» → remember-токен в сессии
   - выход → сессия инвалидирована, гость на /profile → редирект на /auth/login
3. [ ] Доступ гостя: `/profile`, `/orders`, `/orders/{id}` без авторизации → 302 на `/auth/login`
4. [ ] Восстановление пароля (`PasswordResetTest.php`):
   - запрос ссылки для существующего email → статус «отправлено»
   - для несуществующего → тот же ответ (не раскрываем наличие аккаунта)
   - сброс по валидному токену → пароль сменён, юзер залогинен
   - невалидный/просроченный токен → ошибка
5. [ ] ЛК и заказы (`OrderFeatureTest.php`):
   - `/profile` — данные юзера, последние заказы
   - `/orders` — видны только свои заказы; чужой заказ не показывается
   - `/orders/{id}` чужого юзера → 403
   - отмена заказа → статус cancelled; отмена уже отправленного → ошибка
   - повтор заказа → товары добавлены в корзину
6. [ ] Smoke-тесты страниц (`PagesSmokeTest.php`): главная, каталог, карточка товара, корзина, блог (index/show), все pages/*, все auth/* → HTTP 200
7. [ ] Checkout авторизованного (`CheckoutFeatureTest.php`): заказ привязывается к `user_id`, страница success, гость тоже может оформить (user_id = null)
8. [ ] Прогнать `make test` (Unit + Feature), Pint, PHPStan — всё зелёное
9. [ ] Обновить `docs/TECH-DEBT.md` (закрыть пункт «Feature-тесты отсутствуют»), `docs/PAGES.md`/`PROGRESS.md` при необходимости; обновить этот план → завершён

## Открытые вопросы

- 🔍 Токены сброса пароля: письма уходят в лог (MAIL_MAILER=log) — в тестах проверяем через `Password::broker()` напрямую или фабрику токенов, не через почту.

## Журнал

- 2026-08-30: план создан (по предложению Сергея: тестами не занимались после большого блока вёрстки). Реализация — следующим шагом.
