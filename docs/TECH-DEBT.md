# 🐛 Известные технические недоработки (TECH-DEBT)

> Живой список: фиксируем, чтобы агенты и люди знали о долгах и не «открывали их заново».
> Формат: `- [ ] описание — (где, когда замечено)`. Выполненное — переносим в завершённые или удаляем.

## Активные

- [ ] **Нет Blade-шаблонов (12 view)** — сайт отдаёт HTTP 500. Активный план: `docs/plans/2026-08-blade-templates.md`.
- [ ] **PHPStan: 13 ignoreErrors на view-string** в `src/phpstan.neon` — временный компромисс, убрать после создания view.
- [ ] **Шрифт Gilroy** — в макете используется Gilroy, в вебе его нет; решить: подключить (Google Fonts аналог / локально) или заменить на системный стек.
- [ ] **Feature-тесты отсутствуют** — тесты только Unit (286). После вёрстки добавить Feature-тесты страниц (главная, каталог, корзина).
- [ ] **`docker-compose.yml` предупреждает** про устаревший атрибут `version` — убрать при следующей правке compose.

## Завершённые

- [x] ~~Устаревший `.gitignore`: строка `resources/` игнорировала все шаблоны/фронтенд~~ — убрана (2026-08-29, коммит `721590a`).
- [x] ~~Laravel 12 / PHP 8.2 (закончились багфиксы)~~ — апгрейд на Laravel 13 / PHP 8.5 (2026-08-29, коммит `2928045`).
- [x] ~~`auth()->id` без вызова в ReviewController (отдавал null)~~ — исправлено (2026-08-29, коммит `150d6c7`).
